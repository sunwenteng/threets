export enum ATTACK_TYPE {
    NORMAL_ATTACK = 1,
    POWER_ATTACK = 2
}

export enum ATTACK_CONSUME {
    NORMAL_CONSUME = 1,
    POWER_CONSUME = 3
}

export enum RANK_TYPE {
    TOP10 = 1,
    IN_FRIEND = 2,      // 在好友中
    IN_WORLD = 3
}

export enum PVP_FIGHT_TYPE {
    NULL = 0,
    ARENA = 1,
    FRIEND = 2
}